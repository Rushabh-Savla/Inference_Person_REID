services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: person-reid-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/127.0.0.1/6333'"]
      interval: 5s
      timeout: 3s
      retries: 20

  reid:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: person-reid-nvdcf
    depends_on:
      qdrant:
        condition: service_healthy
    network_mode: host
    gpus: all
    ipc: host
    shm_size: "4gb"
    privileged: true
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility,video,graphics
      QDRANT_URL: http://127.0.0.1:6333
      NVDCF_DEEPSTREAM_ROOT: /opt/nvidia/deepstream/deepstream
      NVDCF_TRACKER_LIBRARY: /opt/nvidia/deepstream/deepstream/lib/gst-plugins/libnvds_nvmultiobjecttracker.so
      GST_PLUGIN_PATH: /opt/nvidia/deepstream/deepstream/lib/gst-plugins
    volumes:
      - ./:/workspace/inference_person_reid
      - ~/.insightface:/root/.insightface
      - ~/.cache:/root/.cache
    working_dir: /workspace/inference_person_reid
    stdin_open: true
    tty: true
