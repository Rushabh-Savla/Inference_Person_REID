# Runtime dependencies for the multi-camera person ReID pipeline.
# Install:  pip install -r requirements.txt

# --- Detection + tracking -------------------------------------------------
ultralytics==8.4.89        # YOLO11 detector + ByteTrack tracker

# --- ReID models -----------------------------------------------------------
torch==2.12.1
torchvision==0.27.1
torchreid==0.2.5

# FastReID (CURRENT body backbone) is vendored under src/reid/vendor/fastreid/.
# The CHECKPOINT is not in git because it is larger than GitHub's file limit.

scipy==1.15.3
gdown==5.2.2
tensorboard==2.21.0

# --- Face / ONNX support ---------------------------------------------------
insightface==1.0.1
onnxruntime-gpu==1.28.0

# --- Tracking --------------------------------------------------------------
lap==0.5.13

# --- Vector store ---------------------------------------------------------
qdrant-client==1.18.0

# --- Core numerics / IO ---------------------------------------------------
numpy==2.2.6
opencv-python==5.0.0.93
PyYAML==6.0.3

# tkinter is optional and OS-provided. Headless inference does not require it.

# IMPORTANT:
# insightface declares plain `onnxruntime` as a dependency. That package is
# CPU-only and can replace/precede onnxruntime-gpu in the environment.
# The runtime code requires CUDAExecutionProvider, so remove the CPU package:
#   python -m pip uninstall -y onnxruntime
