# Runtime dependencies for the multi-camera person ReID pipeline.
# Pins reflect the versions verified working in the project venv.
# Install:  pip install -r requirements.txt

# --- Detection + tracking -------------------------------------------------
ultralytics==8.4.89        # YOLO11 detector + ByteTrack tracker

# --- ReID model (appearance embeddings) -----------------------------------
torch==2.12.1
torchvision==0.27.1
torchreid==0.2.5

# FastReID (CURRENT body backbone) is vendored under src/reid/vendor/fastreid/.
# The CHECKPOINT is not in git because it is larger than GitHub's file limit.

scipy==1.15.3
gdown==5.2.2
tensorboard==2.21.0

# --- Face ReID -------------------------------------------------------------
# InsightFace 1.0.1 is the current PyPI release. V4 uses buffalo_l through
# FaceAnalysis. The provided pretrained model packs are for non-commercial
# research use; review licensing before commercial deployment.
insightface==1.0.1
onnxruntime-gpu==1.28.0

# --- Tracking --------------------------------------------------------------
lap==0.5.13

# --- Vector store ---------------------------------------------------------
qdrant-client==1.18.0

# --- Core numerics / IO ---------------------------------------------------
numpy==2.2.6
opencv-python==5.0.0.93
PyYAML==6.0.3

# tkinter is optional and OS-provided. Headless inference does not require it.
